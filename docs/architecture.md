System diagram : Training to model artifact (model.pth, labels.json) to inference-service (FastAPI) to Docker to Kubernetes

Components + responsibilities

Ports used (8000)

What “/health” and “/predict” do